@model Depot.App.ViewModels.ProdutoViewModel

<div style="padding-top: 20px">
    <div>
        <hr />
        <h4>Produto</h4>
    </div>

    @if (Model != null)
    {
        <table class="table table-hover">
            <thead class="thead-dark">
                <tr>
                    <th>
                        @Html.DisplayNameFor(model => model.Descricao)
                    </th>                   
                    <th>
                        @Html.DisplayNameFor(model => model.Quantidade)
                    </th>          
                    <th></th>
                </tr>
            </thead>
            <tr>
                <td>
                    @Html.DisplayFor(model => model.Descricao)
                </td>
                <td>
                    @Html.DisplayFor(model => model.Quantidade)
                </td>               

                <td>
                    <a supress-by-action="Edit,ValidaBaixa" asp-action="BaixaProdutos" asp-route-id="@Model.Id" class="btn btn-info" data-modal="">
                        <span title="Editar" class="fa fa-sign-out-alt"></span>
                    </a>
                </td>
            </tr>


        </table>
    }
</div>